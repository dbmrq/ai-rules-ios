# Template — copy to project root as .periphery.yml and edit scheme/project.
project: YourApp.xcodeproj
schemes:
  - YourApp
retain_objc_accessible: true
retain_swift_ui_previews: true
retain_codable_properties: true
exclude_tests: true
# After first successful scan: ./scripts/deadcode.sh --write-baseline .periphery.baseline.json
# baseline: .periphery.baseline.json
