# XcodeGen: paste onto your application target under preBuildScripts.
# Also set SWIFT_TREAT_WARNINGS_AS_ERRORS (see settings-strict.yml) and
# ENABLE_USER_SCRIPT_SANDBOXING: NO on that target.
#
# preBuildScripts:
#   - name: Quality Check
#     basedOnDependencyAnalysis: false
#     script: |
#       set -euo pipefail
#       cd "${SRCROOT}"
#       ./scripts/check.sh
